# Setup GitHub Pages

**Jangan pilih `main / (root)` pada Pages untuk source React/Vite ini.**

Pilih:

`Settings → Pages → Build and deployment → Source → GitHub Actions`

Workflow yang sudah disediakan akan melakukan build dan deploy otomatis.
